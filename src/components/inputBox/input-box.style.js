import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    inputBoxContainer: {
        marginTop: 5,
        marginBottom: 5,
    },
    TextBox: {
        borderRadius: 4,
        padding: 10,
        fontSize: 18,
        borderWidth: 1,
        backgroundColor: '#fff',
    }
});
